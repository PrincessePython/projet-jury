<h1>Activez votre compte</h1>
<p>Bonjour {{ user }},</p>
<p>Votre inscription sur notre site est a valider. Qliquez sur le lien ci-dessous:</p>
<p><a href="{{ absolute_url(path('verify_user', {token:token})) }}">{{ absolute_url(path('verify_user', {token:token})) }}</a></p>
<p>Ce lient expirera dans 1 heure</p>